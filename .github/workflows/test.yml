name: testAction

on: push
      
jobs:
  testing:
    runs-on: ubuntu-latest
    
    steps:
      - name: printer
        
      - name: testing
        uses: ./.github/actions/testing
        with:
          Name: ${{ github.event.inputs.Name }}
