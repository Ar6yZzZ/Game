name: testAction

on: push
      
jobs:
  testing:
    runs-on: ubuntu-latest
    
    steps:
      - name: printer
        uses: actions/printer@v2
        
      - name: testing
        uses: ./.github/actions/testing
        with:
          Name: ${{ github.event.inputs.Name }}
